{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm install && FAST_REFRESH=false NODE_OPTIONS=--max-old-space-size=4096 npm run build"
  },
  "deploy": {
    "startCommand": "node server.js"
  }
}
